services:
  # nestjs:
  #   build:
  #     context: .
  #     args:
  #       - HTTP_PROXY=http://karelmb:03032766003@172.28.0.2:8080
  #       - HTTPS_PROXY=http://karelmb:03032766003@172.28.0.2:8080
  #   ports:
  #     - "3000:3000"
  #   links:
  #     - monguito

  postgres:
    image: postgres:latest
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=karel
      - POSTGRES_PASSWORD=12345678
      - POSTGRES_DB=my_db
    volumes:
      - postgres_data:/var/lib/postgresql
volumes:
  postgres_data:
  # mongo-data: